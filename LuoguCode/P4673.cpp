#include <iostream>
using namespace std;
constexpr int MAXN = 5e4 + 5;
int s[MAXN],t[MAXN],a[MAXN],b[MAXN],c[MAXN],d[MAXN];

int main(){
    int N,M,P,T;
    cin >> N >> M >> P >> T;
    for (int i = 1; i <= M; i++){
        cin >> s[i] >> t[i] >> a[i] >> b[i] >> c[i] >> d[i];
    }
    
    return 0;
}